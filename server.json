{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-09-29/server.schema.json",
  "name": "dev.domainee/domainee",
  "description": "Custom domains API for SaaS: connect, verify, and manage customer domains, SSL, and DNS",
  "version": "1.0.2",
  "websiteUrl": "https://domainee.dev/mcp",
  "remotes": [
    {
      "type": "streamable-http",
      "url": "https://mcp.domainee.dev/mcp",
      "headers": [
        {
          "name": "Authorization",
          "description": "Optional. Bearer token using a Domainee API key, e.g. `Bearer sk_live_...`. Without it the 18 free diagnostic tools still work and tools/list enumerates them; with it you also get the 12 workspace tools. Create one at https://domainee.dev/developers",
          "isRequired": false,
          "isSecret": true
        }
      ]
    }
  ]
}
